export const PokemonModel = (
  id,
  name,
  height,
  weight,
  imageUrl,
  types,
  stats
) => ({
  id: id,
  name: name,
  height: height,
  weight: weight,
  imageUrl: imageUrl,
  types: types,
  stats: stats,
});
